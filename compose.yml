
services:

  mongo:
    image: mongo:7
    container_name: etl_mongo
    restart: unless-stopped
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongo_data:/data/db
    env_file:
      - .env
    networks:
      - etl_network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis/redis-stack:latest
    container_name: etl_redis
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - REDIS_ARGS=--port 6379 --bind ${REDIS_BIND} --protected-mode ${REDIS_PROTECTION_MODE}
    ports:
      - "${REDIS_PORT}:6379"
    networks:
      - etl_network
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
  
  celery_worker:
    build: .
    container_name: etl_worker
    restart: unless-stopped
    command: celery -A data_pipelines.core.celery.celery_app worker --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - redis
      - mongo
    env_file:
      - .env
    networks:
      - etl_network

  celery_beat:
    build: .
    container_name: etl_beat
    restart: unless-stopped
    command: celery -A data_pipelines.core.celery.celery_app beat --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - redis
      - mongo
    env_file:
      - .env
    networks:
      - etl_network

volumes:
  mongo_data:

networks:
  etl_network:
    external: false